!!!
%html
	%head
		%title= "Homicide Site | #{@title}"
		= stylesheet_link_tag "application", media: "all"
		= javascript_include_tag "application"
		= csrf_meta_tags
	%body.the_board
		= render 'layouts/banner'
		.main
			= yield
			= debug(params) if Rails.env.development?